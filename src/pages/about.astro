---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getEntry } from 'astro:content';
import AboutEditor from '../components/editor/AboutEditor';
import EditableText from '../components/editor/EditableText';

const rawBase = import.meta.env.BASE_URL ?? '/';
const base = rawBase === '/' ? '' : rawBase.replace(/\/$/, '');

const aboutEntry = await getEntry('pages', 'about');
const portrait = aboutEntry?.data.portrait ?? '/images/about/lia-paz.webp';
const statement = aboutEntry?.data.statement ?? aboutEntry?.data.quote ?? `Estar vivo es una experiencia escalofriante;
efímera.
Estar vivo es un acto político.

Yo estoy viva, mi arte habla de vivir cerca de la muerte.`;

const semblanza = aboutEntry?.data.semblanza ?? (aboutEntry?.body && aboutEntry.body.trim().length > 0 ? aboutEntry.body : `Mi producción se inclina al campo de lo figurativo, centrándose en la representación del rostro como territorio donde la fragilidad y la fuerza conviven, construyendo imágenes intensas que dialogan con la salud mental, la tensión interna, la fugacidad del momento: Carpe Diem, et Memento Mori.`);

const siteUrl = Astro.site ? Astro.site.toString().replace(/\/$/, '') : 'https://fractalia-iota.vercel.app';
const aboutSchema = {
  '@context': 'https://schema.org',
  '@type': 'AboutPage',
  '@id': `${siteUrl}/about#webpage`,
  url: `${siteUrl}/about`,
  name: 'Sobre Lía Paz · Biografía y Trayectoria Artística',
  description: 'Biografía, trayectoria y filosofía pictórica de la artista visual mexicana Lía Paz.',
  isPartOf: { '@id': `${siteUrl}/#website` },
  mainEntity: { '@id': `${siteUrl}/#artist` },
  inLanguage: 'es-MX',
};
---

<BaseLayout
  title="Sobre Lía Paz · Biografía y Filosofía Artística | Fractalia"
  description="Conoce la trayectoria, visión plástica y filosofía de la artista visual Lía Paz (Fractalia). Pintura figurativa contemporánea y el arte de inmortalizar instantes."
  image={portrait}
  imageAlt="Retrato de la artista visual Lía Paz"
  ogType="profile"
  jsonLd={aboutSchema}
>
  <!-- Page Header -->
  <section class="py-12 sm:py-16 md:py-24 border-b border-neutral-100 bg-neutral-50/50">
    <div class="max-w-4xl mx-auto px-6 text-center">
      <EditableText
        client:load
        contentKey="about.header.label"
        defaultText="Perfil & Trayectoria"
        as="span"
        className="font-sans text-[11px] tracking-[0.3em] uppercase text-neutral-400 font-medium block mb-3"
      />
      <EditableText
        client:load
        contentKey="about.header.title"
        defaultText="Sobre la Artista"
        as="h1"
        className="font-serif text-4xl sm:text-5xl md:text-6xl text-neutral-900 font-normal tracking-wide block"
      />
    </div>
  </section>

  <!-- Bio Main Section (Sean Layh style 2-column layout with In-Situ Editing) -->
  <section class="py-12 sm:py-16 md:py-24 bg-white">
    <div class="max-w-6xl mx-auto px-6 sm:px-8 lg:px-12">
      <AboutEditor
        client:load
        initialPortrait={portrait}
        initialStatement={statement}
        initialSemblanza={semblanza}
        base={base}
      />
    </div>
  </section>
</BaseLayout>

